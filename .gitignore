# Dependencies
node_modules/
.pnp.*

# Build output
dist/
build/
*.tsbuildinfo

# Env
.env
.env.demo
.env.local
.env.*.local

# Prisma
apps/api/prisma/generated/

# Logs
*.log
npm-debug.log*

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Docker volume mounts (if any local)
.docker-data/

# Coverage
coverage/

# Database dumps (never commit — contains real data)
*.dump
# NB: do NOT ignore *.sql broadly — Prisma migration files are .sql and MUST be committed.
# Ignore only ad-hoc dump SQL at the repo root.
/*.sql

# Throwaway diagnostic scripts (one-off Amazon/DB probes). Never part of the build.
apps/api/.tmp/
